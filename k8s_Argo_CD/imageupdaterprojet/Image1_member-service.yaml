apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: member-service-updater
  namespace: argocd
spec:
  namespace: argocd
  applicationRefs:
    - namePattern: "app-member-service"
      images:
        - alias: "member-service"
          imageName: "brahimbtf/member-service:1.x"
          commonUpdateSettings:
            allowTags: "regexp:^1\\.[0-9]+$"
            updateStrategy: "semver"
            forceUpdate: true

  writeBackConfig:
    method: "git:secret:argocd/git-creds"
    gitConfig:
      repository: "https://github.com/BOUTFARJOUTE-BRAHIM/k8s_Argo_CD.git"
      branch: "main"
      writeBackTarget: "kustomization:."
